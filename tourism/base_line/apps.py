from __future__ import unicode_literals

from django.apps import AppConfig


class BaseLineConfig(AppConfig):
    name = 'tourism.base_line'
    verbose_name = 'tourism.base_line'
    label = 'tourism.base_line'
